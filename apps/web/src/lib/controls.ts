// Quiet zinc control style shared by page-level control rows (Bank's month
// switcher, Calendar's controls) so similar functionality looks identical
// across pages: plain text that gains a background on hover.
export const quietBtnClass =
  "rounded-md px-2 py-1 text-zinc-400 hover:bg-zinc-800 hover:text-zinc-100";
